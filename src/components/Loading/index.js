import React, { Fragment } from "react";

const Loading = ({message}) => {
    return(
        <Fragment>
            <h1>
                Loading...
            </h1>
        </Fragment>
    )
}

export default Loading;